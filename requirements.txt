# Kern-Abhaengigkeiten: nur pytest, keine externen Laufzeit-Dependencies fuer
# or_set.py / node.py - die CRDT-Implementierung selbst ist reines Python.
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Optional: nur noetig fuer den echten Message-Broker-Teil (broker.py,
# broker_demo.py, tests/test_broker_integration.py). Ohne diese Zeile und
# ohne laufenden NATS-Server laeuft die Kern-Testsuite trotzdem vollstaendig
# (die Broker-Tests werden automatisch uebersprungen, siehe README).
nats-py>=2.6.0
